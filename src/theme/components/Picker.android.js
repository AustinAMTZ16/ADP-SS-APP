import {variable} from "../";

export default (variables = variable) => {
	let pickerTheme = {
    ".note": {
      color: "#8F8E95"
    },
    width: 90,
    marginRight: -4
  };

  return pickerTheme;
};
