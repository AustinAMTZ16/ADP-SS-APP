const initialState = {
  appDrawerType: 'OOOO',
};

export default function( state: State = initialState, action: Action ): State {


  if( action.type === 'DRAWER_TYPE' ) {
    return {
      ...state,
      appDrawerType: action.appDrawerType
    };
  }


  return state;
}
